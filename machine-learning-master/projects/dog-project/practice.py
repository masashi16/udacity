import cv2
from matplotlib import pyplot as plt
from glob import glob



"""
for img_file in myimages:
    print(img_file)
    img = cv2.imread(img_file)
    cv2.imshow('image', img)
    cv2.waitKey(0)
    cv2.destroyAllWindows()
"""
